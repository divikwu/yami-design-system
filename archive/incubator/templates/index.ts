/**
 * Page templates barrel.
 *
 * Phase 6 ships web templates only. App (mobile) templates need a TabBar
 * + TopBar component pair which isn't built yet; they'll land when those
 * primitives do, under pages/templates/app/.
 */

export * from './web'

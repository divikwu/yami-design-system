# Documentation before the team-guide migration

The `docs/` subtree preserves the 13 Chinese/English document pairs replaced on
2026-08-31. It is an editorial archive, not a published collection or a current
source of usage instructions. The live collection is `content/docs/`.

`getting-started` keeps its existing URL with the new library-usage introduction.
Other old URLs are mapped in `lib/legacy-doc-redirects.json` to current guides or
the corresponding Storybook specifications. Search, pagination, and the sitemap
include only the live collection. Keep these archived files for recovery and
source comparison; do not link new tutorials to them as current guidance.

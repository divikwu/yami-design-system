{
  "$schema": "../../schema/component.json",
  "name": "Billboard",
  "category": "composite",
  "status": "stable",
  "version": "0.1.0",
  "description": "Full-bleed promotional band whose entire content is one campaign image, wrapped in a single link.",
  "figma": {
    "nodeId": null,
    "componentKey": null,
    "notes": "Modelled on the production yami.com homepage floating bar, which renders a 1585x160 artwork directly under the shortcut rail."
  },
  "exports": [
    "Billboard",
    "BillboardArtwork",
    "BillboardImage",
    "BillboardProps"
  ],
  "composes": [],
  "props": {
    "image": {
      "type": "BillboardImage",
      "required": true,
      "description": "Campaign artwork with its intrinsic `width`/`height`, which reserve the band's height before it loads. `mobile` swaps in narrow artwork below 1024px, carrying its own dimensions. `alt` is usually empty because `label` already names the offer."
    },
    "href": {
      "type": "string",
      "required": true,
      "description": "Destination for the band."
    },
    "label": {
      "type": "string",
      "required": true,
      "description": "Localized accessible name. Required: the artwork's words are pixels, so without it the band is an unnamed link."
    },
    "imageLoading": {
      "type": "\"eager\" | \"lazy\"",
      "required": false,
      "default": "lazy",
      "description": "Artwork loading strategy; eager for a band above the fold."
    }
  },
  "rules": {
    "focus": {
      "ruleId": "focus-style",
      "note": "The link carries a token-backed focus ring at the band's radius."
    },
    "radius": {
      "ruleId": "no-custom-radii",
      "note": "Consumes --radius-surface-default, matching the other homepage section cards."
    },
    "tapTarget": {
      "ruleId": "tap-target",
      "note": "The whole band is the target, far above the 44px minimum."
    }
  },
  "tokens": [
    "--surface-primary",
    "--border-focus",
    "--stroke-thick",
    "--radius-surface-default",
    "--font-family-ios",
    "--layout-page-margin-card",
    "--space-025",
    "--space-400",
    "--space-600"
  ],
  "tokenBindings": {
    "--surface-primary": {
      "property": "background",
      "selector": ".root"
    },
    "--radius-surface-default": {
      "property": "border-radius",
      "selector": ".link and .root below 1024px"
    },
    "--layout-page-margin-card": {
      "property": "margin-inline",
      "selector": ".root below 1024px"
    },
    "--space-600": {
      "property": "padding-inline",
      "selector": ".root from 1024px"
    }
  },
  "accessibility": {
    "role": "labelled region containing a single link",
    "wcagLevel": "AA",
    "tapTargetMin": 44,
    "keyboardNav": [
      "tab",
      "enter"
    ],
    "ariaPattern": "section[aria-label] wrapping one a[aria-label] around decorative artwork",
    "structure": "The label names both the region and the link inside it. Naming only the region leaves the link nameless, since its only content is artwork with empty alt and a link takes its name from its content.",
    "interaction": "One link covers the band; there is no secondary target."
  },
  "dependencies": {
    "runtime": [
      "react"
    ],
    "peer": [],
    "devOnly": []
  },
  "usageRef": "./usage.md",
  "examples": [
    "./examples.tsx#BillboardExample"
  ]
}

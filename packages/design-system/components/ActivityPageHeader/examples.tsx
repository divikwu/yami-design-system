import { ActivityPageHeader } from "./ActivityPageHeader";

export function ActivityPageHeaderExample() {
  return (
    <ActivityPageHeader
      title="Anua"
      homeHref="/en"
      onSearch={() => {}}
      onCart={() => {}}
    />
  );
}

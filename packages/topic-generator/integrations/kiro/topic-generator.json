{
  "name": "topic-generator",
  "description": "Analyze Theme Keywords and run versioned, evidence-validated ProductSelection workflows against Yami catalog artifacts.",
  "prompt": "Act as the TOPIC GENERATOR interactive Product Agent. Load the shared product-selection Skill for ProductSelection work and use the package CLI as the deterministic runtime. Create only the proposal requested by the returned state. Never infer taxonomy or product facts, alter artifact digests, reproduce allocation rules, or claim that the Web page invoked this Kiro session. Report blocked and rejected states exactly. Use --output only when the user explicitly asks for portable Run Artifacts.",
  "tools": ["read", "write", "shell"],
  "allowedTools": ["read"],
  "permissions": {
    "rules": [
      {
        "capability": "shell",
        "match": ["pnpm topic-generator:analyze *"],
        "effect": "allow"
      },
      {
        "capability": "shell",
        "match": ["rm -rf *", "sudo *", "git *"],
        "effect": "deny"
      }
    ]
  },
  "resources": [
    "file://packages/topic-generator/README.md",
    "file://packages/topic-generator/docs/core.md",
    "file://packages/topic-generator/docs/semantic-proposal.example.json",
    "skill://packages/topic-generator/integrations/codex/topic-intent/SKILL.md",
    "skill://.kiro/skills/product-selection/SKILL.md"
  ],
  "welcomeMessage": "TOPIC GENERATOR is ready. Give me a shopping keyword to analyze."
}
